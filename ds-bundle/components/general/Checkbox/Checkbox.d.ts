import * as React from 'react';

/**
 * Checkbox — from powerup-menu-website@0.1.0.
 * @replaces input[type=checkbox]
 */
export interface CheckboxProps {
  [key: string]: unknown;
}

export declare const Checkbox: React.ComponentType<CheckboxProps>;
