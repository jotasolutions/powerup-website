Command from powerup-menu-website. Use via `window.PowerUpMenuDS.Command` (bundle loaded from the root `_ds_bundle.js`).

## Related

`CommandDialog`, `CommandEmpty`, `CommandGroup`, `CommandInput`, `CommandItem`, `CommandList`, `CommandSeparator`, `CommandShortcut`
